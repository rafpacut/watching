#!/bin/bash

touch $USER.start

getopts ":v" args

if [ $args = "v" ]; then
	for i in * 
	do
		if [ ".start" < $i ]; then
			vlc $i
		fi
	done
else
	for i in * 
	do
		if [ ".start" < $i ]; then
			mplayer $i
		fi
	done
fi

touch $USER.end
