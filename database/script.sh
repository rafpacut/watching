#!/bin/bash

#for i in {1..44}
#do
#	i=1
#	name=`sed -n "$ip" sth2`
#	echo "name: $name"
#	status=`echo $name | cut -c -7`
#	echo "status: $status"
#	
#	if [ "$status" = "[32;48m" ]; then
#		number=`cat $name/$USER.start`
#		sed -i "\$as_$number$name/" .database
#	fi
#
#
#	if [ "$status" = "[30;48m" ]; then
#		sed -i "\$an__/$name/" .database
#	fi
#
#	if [ "$status" = "[31;48m" ]; then
#		sed -i "\$ad__/$name/" .database
#	fi
#done
	lol=1
	name=`sed -n "$lolp" sth2`
	echo $name
